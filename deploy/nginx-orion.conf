# /etc/nginx/sites-available/orion
# Domínio: orionaii.com → app Docker na porta 3001
# IMPORTANTE: copiar proxy_read_timeout também no bloco server { listen 443 ssl; }

server {
    listen 80;
    listen [::]:80;
    server_name orionaii.com www.orionaii.com;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}

# Exemplo HTTPS (certbot) — timeouts obrigatórios para SSE/chat:
# server {
#     listen 443 ssl http2;
#     server_name orionaii.com www.orionaii.com;
#     ...
#     location / {
#         proxy_pass http://127.0.0.1:3001;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_buffering off;
#         proxy_read_timeout 300s;
#         proxy_send_timeout 300s;
#     }
# }
